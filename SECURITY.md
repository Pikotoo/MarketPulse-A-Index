# Security Policy

## 报告漏洞

如发现安全漏洞，请发送邮件至项目维护者，勿公开提交 Issue。

## 安全注意事项

1. **API Key 管理**：生产环境中务必修改 `.env` 中的默认 Key，并定期轮换
2. **数据隐私**：本项目不收集用户数据，所有数据存储在本地
3. **网络隔离**：建议在内网或 VPN 后部署，避免公网暴露
4. **限速保护**：默认启用四级限速，防止 API 滥用
5. **依赖审计**：定期运行 `pip-audit` 检查依赖漏洞
